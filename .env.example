# SMTP konfiguration
# Kopier denne fil til .env og udfyld med dine egne værdier

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=din@email.dk
SMTP_PASS=dit-password

# Server port (standard: 3000)
PORT=3000
